import React, { useState } from 'react';

const Explore = () => {
    return (   <div>
                <h1>Explore</h1>
                <input type="text" placeholder="Search..." />
        </div>
    );
};

export default Explore;
